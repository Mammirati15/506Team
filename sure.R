# Define the number for multiplication
number <- 5

# Initialize an empty vector to store results
results <- numeric(10)

# Perform multiplication using a for loop
for (i in 1:10) {
  results[i] <- number * i
}

# Print the results
print(results)

